import styled from 'styled-components';

export const Wrapper = styled.div`
  color: var(--gray-color);
  font-size: 20px;
  font-style: normal;
  font-weight: 400;
  line-height: normal;
  text-align: end;
  transition: 0.5s;
`;
